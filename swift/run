echo "############################################"
echo "# swift run hello                          #"
echo "#           basic                          #"
echo "#           character                      #"
echo "#           collection                     #"
echo "############################################"

read -p "swift run " name

if [[ -n "$name" ]]; then
	echo "Run ${name}"
	swift run $name
fi
